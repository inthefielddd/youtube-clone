extends layout/main

block content
    .video__player
        video(src=`/${video.fileUrl}`)
    .video__info
        a(href=routes.editVideo)
        h5.video__title=video.title
        p.video__description=video.description   
        h6.video__views=video.views
