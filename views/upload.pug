extends layout/main

block content
    .form-container
        form(action=routes.upload method="post")
            label(for="fiel") VideoFile
            input(type="file") 
            input(type="text" name="title" placeholder="Title")
            textarea(name="description" placeholder="Description")
            input(type="submit" value="Upload")
