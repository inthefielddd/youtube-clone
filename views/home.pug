extends layout/main
include mixins/videoBlock

block content
    .videos
        each item in videos
            +videoBlock({
                title:item.title,
                description:item.description,
                videoFile:item.fileUrl,
                views:item.views  })